@extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">ClassificadoItem {{ $classificadoitem->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/classificado-item') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/classificado-item/' . $classificadoitem->id . '/edit') }}" title="Edit ClassificadoItem"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/classificadoitem', $classificadoitem->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete ClassificadoItem',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $classificadoitem->id }}</td>
                                    </tr>
                                    <tr><th> Category Id </th><td> {{ $classificadoitem->category_id }} </td></tr><tr><th> Titulo </th><td> {{ $classificadoitem->titulo }} </td></tr><tr><th> Alias </th><td> {{ $classificadoitem->alias }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
